package com.example.spinwheel;

import com.example.spinwheel.base.domain.Schedule;
import com.example.spinwheel.base.domain.TotalScorehalf;
import com.example.spinwheel.utils.LoggerManager;
import org.springframework.stereotype.Component;

@Component
public class StatisticBo {
    private static LoggerManager logger = LoggerManager.getLogger(StatisticBo.class);

    public Boolean checkTotalScore(TotalScorehalf totalScorehalf, Schedule schedule) {
        Boolean result = false;
        if ()
    }
}
